{
  "name": "@cara/demo-component",
  "description": "A component managed by porter-cli",
  "version": "4.3.4",
  "private": true,
  "dependencies": {
    "yen": "1.2.4"
  },
  "license": "MIT",
  "devDependencies": {
    "@cara/porter-cli": "^4.3.4",
    "expect.js": "^0.3.1",
    "mocha": "^9.2.2"
  },
  "scripts": {
    "start": "rm -rf public && porter serve --paths .",
    "test": "rm -rf public && DEBUG=porter,$DEBUG porter serve --paths . --headless"
  }
}
