{
  "name": "@cara/porter-workspace",
  "private": true,
  "devDependencies": {
    "@babel/core": "^7.15.8",
    "@babel/eslint-parser": "^7.15.8",
    "eslint": "^8.1.0",
    "expect.js": "^0.3.1",
    "lerna": "^2.11.0",
    "mocha": "^5.0.0",
    "node-dev": "^3.1.3",
    "nyc": "^13.1.0",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "test": "lerna bootstrap && lerna link && rm -rf .nyc_output && lerna run coverage && nyc report --reporter=lcov"
  }
}
