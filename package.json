{
  "name": "porter-universe",
  "private": true,
  "devDependencies": {
    "expect.js": "^0.3.1",
    "heredoc": "^1.3.1",
    "lerna": "^2.8.0",
    "mocha": "^5.0.0",
    "supertest": "^3.0.0"
  },
  "scripts": {
    "test": "lerna bootstrap && lerna link && lerna run test"
  }
}
