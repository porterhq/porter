{
  "name": "@cara/demo-cli",
  "description": "A demo of porter-cli",
  "version": "4.0.0-beta.6",
  "private": true,
  "dependencies": {
    "cropper": "^3.0.0",
    "jquery": "^3.3.1",
    "prismjs": "^1.7.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@cara/porter-cli": "^4.0.0-beta.6",
    "expect.js": "^0.3.1",
    "mocha": "^9.1.1"
  },
  "scripts": {
    "start": "rm -rf public && DEBUG=porter,$DEBUG porter serve",
    "test": "rm -rf public && DEBUG=porter,$DEBUG porter serve --headless"
  }
}
