#!/usr/bin/env node

'use strict'

const porter = require('../../..')

Promise.all([
  porter.compileAll(),
  porter.compileStyleSheets({ match: 'stylesheets/app.css' })
])
  .then(function() {
    console.log('done')
  })
  .catch(function(err) {
    console.error(err.stack)
  })
